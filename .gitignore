# Capacitor / Android — artefakty buildów i ustawienia lokalne
android/.gradle/
android/.idea/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
android/capacitor-cordova-android-plugins/build/
android/app/google-services.json
# Faza 4 / D2 � podpis release (hasla NIGDY do repo)
android/keystore.properties
*.jks
*.keystore
node_modules/
www/
# Biblioteki kopiowane z node_modules przez scripts/vendor.mjs.
# Generowane przy kazdym `npm run build:app`, wersje przypiete
# w package.json — nie ma powodu trzymac ich w repozytorium.
vendor/
